 <div class="container">
    <div class="row">
        {{#each imageArray}}
        <div class="col-md-6">
            <img src="/images/{{../city}}/{{../city}}{{this}}.jpg"/>
        </div>
        {{/each}}
    </div>
</div>
